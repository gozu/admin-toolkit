{
 "description": "Phase C groundedness gate for the scoping-architect agent. Expected values verified against live tam-global payloads on 2026-07-02 (shapes dir). Every agent answer must cite host + tool; checker greps for the expected facts.",
 "host": "local (tam-global hub)",
 "questions": [
  {
   "q": "What DSS version and OS does the local host run, and when was it last restarted?",
   "expect": [
    "14.7.0",
    "AlmaLinux 9.8"
   ],
   "source": "instance_health system"
  },
  {
   "q": "Which Claude models are available in the LLM Mesh and on which connections?",
   "expect": [
    "Claude",
    "opus"
   ],
   "source": "config_inspect llms name_filter=claude \u2014 akaos drifted 2026-07-03: kaosclaude/bhavikbedrock connections are gone, one 'Claude' connection remains; pin the connection identity + opus family only"
  },
  {
   "q": "How many code environments run deprecated Python versions (2.x, 3.6 or 3.7)? If there are none, answer exactly 'none are deprecated'.",
   "expect": [
    "none are deprecated"
   ],
   "source": "config_inspect code-envs \u2014 akaos drifted 2026-07-03: the 26 deprecated envs were deleted/upgraded (pythonVersionCounts now 3.9\u20133.14 only); the golden now checks the zero-state answer via a prompted echo"
  },
  {
   "q": "What is the largest project by storage and how big is it?",
   "expect": [
    "PYTHONAUDIT_TEST"
   ],
   "source": "akaos drifted 2026-07-03 (Story removal / cleanup wave): largest is PYTHONAUDIT_TEST (~9GB, was 12.2GB); pin the project key only"
  },
  {
   "q": "How many Kubernetes clusters does the local host know about and how many are actually reachable?",
   "expect": [
    "fe-sandbox-cluster"
   ],
   "source": "akaos drifted 2026-07-03 (Story removal / cleanup wave): clusters: fe-sandbox-cluster + feunmanaged running, 1 unavailable, counts drift; pin the sandbox cluster id only"
  },
  {
   "q": "What did we spend on LLMs in the covered audit window, and which project drives it?",
   "expect": [
    "ADMINTOOLKIT"
   ],
   "source": "top LLM spender in the rolling window is the agents' own project (agent runs themselves); ADMINTOOLKIT since the 0.4.658 AGENTOPS merge (akaos rebuilt from scratch 2026-07-07, old AGENTOPS usage gone); pin project identity, never $ amounts"
  },
  {
   "q": "Is the DSS runtime database on PostgreSQL?",
   "expect": [
    "PostgreSQL"
   ],
   "source": "instance_health score path / settings raw internalDatabase"
  },
  {
   "q": "Is Agent Hub installed on the local host and at what version?",
   "expect": [
    "1.2.3"
   ],
   "source": "akaos drifted 2026-07-03 (Story removal / cleanup wave): agent-hub v1.2.3 installed (golden previously pinned 1.4.1)"
  },
  {
   "q": "Which remote hosts can this toolkit reach, and is the Admin Toolkit installed on them?",
   "expect": [
    "local"
   ],
   "source": "akaos drifted 2026-07-03 (Story removal / cleanup wave): remote host presets removed \u2014 only the local host remains; expect a truthful local-only answer"
  }
 ]
}
