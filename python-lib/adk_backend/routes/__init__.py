"""Flask Blueprint route modules for the Admin Toolkit backend.

One module per feature group; each exposes `bp`, registered by backend.py.
Route paths, methods, function names and response shapes are identical to
their original backend.py definitions — these are pure moves.
"""
