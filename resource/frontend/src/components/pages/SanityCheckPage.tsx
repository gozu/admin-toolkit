import { SanityCheckCard } from '../SanityCheckCard';

export function SanityCheckPage() {
  return (
    <div className="w-full py-4 flex flex-col gap-4">
      <SanityCheckCard />
    </div>
  );
}
