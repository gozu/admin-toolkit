/* Agent tool: registry-backed per-domain DSS configuration/insights summaries */
{
    "meta": {
        "label": "Admin Toolkit: Config inspect",
        "description": "Inspect a DSS host by domain (registry-backed: connections + usage/owners, code envs, plugins, LLMs, users, adoption, cost detail, per-project listings). domain='list' returns the manifest.",
        "icon": "icon-cogs"
    },
    "params": []
}
