"""Runtime-ops actuator actions (jobs, scenarios, webapp backends, notebooks,
continuous activities). Filled by the runtime-long-tail phase; the module
exists so the registry's domain list is stable from day one."""

SPECS = []
